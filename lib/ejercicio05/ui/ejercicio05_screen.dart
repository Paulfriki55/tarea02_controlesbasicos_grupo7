import 'package:flutter/material.dart';

class Ejercicio05Screen extends StatelessWidget {
  const Ejercicio05Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ejercicio 5'),
      ),
      body: const Center(
        child: Text('Contenido del Ejercicio 5'),
      ),
    );
  }
}

